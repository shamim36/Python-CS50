import random  # import everything from the random module
from random import choice  # import only the choice function from the random module

# coin = random.choice(["heads","tails"])
coin = choice(["heads","tails"])
print(coin)